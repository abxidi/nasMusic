# 紫云音乐 UI 稿

这是根据 `synology-nas-music-app-design.md` 产出的静态移动端 UI 画板。

## 文件

- `index.html`：核心页面 UI 稿总览
- `styles.css`：视觉规范、组件样式、移动端画板布局
- `app.js`：播放按钮与唱片旋转的轻量交互

## 预览

直接用浏览器打开 `index.html` 即可。页面不依赖外部 CDN、构建工具或网络资源。
